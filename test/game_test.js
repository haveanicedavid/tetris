// var assert = require('chai').assert;
// var Block  = require('../lib/block');
// var Board  = require('../lib/board');
// var game   = require('../lib/game')

// Write a test that
// Creates a new board
// Starts the game somehow?
// Populates a block onto the board
// changes the position of the block
  // On a time interval

// describe('The game runner', function() {
  
  // it('has a timer', function () {
  //  var game = new Game();
  //  game.start();
  //  setTimeout(1500);
  //  assert.equal(game.board.board[3][4], 1);
  // });

  // it('should start with a block at position 0, 5', function (done) {
  //   var board = new Board();
  // });

// });