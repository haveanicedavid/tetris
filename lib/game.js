module.exports = function Game (argument) {
  this.board = new Board();

  // 
  // some function that runs the game with a timer
  // On each click of the timer, a game piece tries to move downward
}